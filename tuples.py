# A tuple is a type of data structure where we can store different values but a tuple is different from a list in the sense that the tuple is immutable.
# Immutable means that the tuple can't be changed or modified

coordinates = (4,5)
print(coordinates)
print(coordinates[0]) 

